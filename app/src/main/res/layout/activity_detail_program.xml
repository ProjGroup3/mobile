<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_img_blue"
    tools:context=".DetailProgramActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/detail_program_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="@color/colorBlack1"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:title="Detail Program"
        app:titleTextColor="@color/colorWhite1" />

    <androidx.cardview.widget.CardView
        android:id="@+id/detail_program_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/detail_program_toolbar"
        android:layout_margin="20dp"
        android:elevation="4dp"
        app:cardCornerRadius="6dp"
        app:cardPreventCornerOverlap="true">

        <ImageView
            android:id="@+id/detail_program_img"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_gravity="center_horizontal"
            android:scaleType="centerCrop"
            android:src="@drawable/photo_1" />

        <TextView
            android:id="@+id/detail_program_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="120dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:text="deskripsi" />

    </androidx.cardview.widget.CardView>

</RelativeLayout>